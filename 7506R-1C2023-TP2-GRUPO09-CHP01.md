---
jupyter:
  jupytext:
    cell_metadata_filter: -all
    formats: ipynb,md
    text_representation:
      extension: .md
      format_name: markdown
      format_version: '1.3'
      jupytext_version: 1.14.5
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

```python
try:
  import google.colab
  IN_COLAB = True
except:
  IN_COLAB = False
import pandas as pd 
import numpy as np

import sklearn
from sklearn.model_selection import train_test_split
from sklearn.feature_extraction.text import CountVectorizer
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.naive_bayes import MultinomialNB
from sklearn.pipeline import make_pipeline
from os.path import exists
# import nltk
# import sys
# print(sys.version)
```

```python
SEED=9
```

```python
reviewDfOriginal = pd.read_csv("./review_train.csv")
reviewDf = reviewDfOriginal.copy()
```

```python
review_pruebasOriginal = pd.read_csv("./review_test.csv")
review_pruebas = review_pruebasOriginal.copy()
```

```python
# vectorizer = CountVectorizer()
# X_train_counts = count_vect.fit_transform(reviewDf["review_es"])
# X_train_counts.shape
# X_train_counts

# =hotelsdf_modelo.drop(['is_canceled'], axis='columns', inplace=False)
reviewDf = reviewDf.drop(["ID"],  axis='columns', inplace=False)
reviewDf
```

```python
reviewDf_x = reviewDf.drop(["sentimiento"],  axis='columns', inplace=False)

reviewDf_y = reviewDf['sentimiento'].copy()

x_train, x_test, y_train, y_test = train_test_split(reviewDf_x,
                                                    reviewDf_y,
                                                    test_size=0.30,
                                                    random_state=SEED,
                                                    shuffle=True
                                                    )
```

```python
reviewDf_x
```

```python
type(reviewDf["sentimiento"])
```

```python
type(reviewDf.sentimiento)
```

```python
# vectorizer = TfidfVectorizer(smooth_idf=True)

# listaDeReviews = reviewDf["review_es"].tolist()[1:3]
# # listaDeReviews = reviewDf["review_es"].tolist()

# vectorizador = vectorizer.fit_transform(listaDeReviews)
```

```python
x_train
```

```python
y_train
```

```python
modelo1 = make_pipeline(TfidfVectorizer(), MultinomialNB())

modelo1.fit(x_train.review_es, y_train)

prediccion = modelo1.predict(x_test.review_es)
```

```python
prediccion
```

```python
prediccion.size
```

```python
review_pruebasOriginal
```

```python
x_train
```

```python
prediccion1 = modelo1.predict(review_pruebas.review_es)
```

```python
review_pruebasOriginal.review_es
```

```python
prediccion1.size
```

```python
df_submission = pd.DataFrame({'id': review_pruebasOriginal['ID'], 'sentimiento': prediccion1})
```

```python
df_submission
```

```python
if not exists('submissions/naiveBayes-0.csv'):
#     y_pred = knn_base.predict(hotelsdf_pruebas)
    prediccion1 = modelo1.predict(review_pruebas.review_es)
    df_submission = pd.DataFrame({'id': review_pruebasOriginal['ID'], 'sentimiento': prediccion1})
    df_submission.to_csv('submissions/naiveBayes-0.csv', index=False)
```

```python
print("sus")
```
